# epson-waste-counter-tool
 Windows Python tool to read and reset waste ink counters on supported Epson USB printers. Detects the model, can   target a printer by serial number, reads counters, and blocks reset until the user confirms the waste pads were   physically cleaned or replaced.
